---
name: seedance-short-drama-director
description: 为 Seedance 2.0 设计、编译和修复中文真人微短剧视频方案，适用于剧情拆镜、演员表演、镜头语言、多人调度、镜间首尾帧衔接、连续生成、参考素材绑定和失败诊断；不用于非 Seedance 模型或纯图片提示词。
---

# Seedance 2.0 中文微短剧导演

把剧情意图编译成 Seedance 可见、可执行、可延续的导演指令。优先保证真人感、叙事清晰和稳定出片，不做形容词堆砌。

## 工作入口

先判断请求属于哪一类：

- 单镜生成：完成 Director's Read、复杂度评分、表演与镜头编译。
- 连续剧情：先建连续性账本和镜间接缝表，再逐镜生成；只把已验收画面写入正史，并用真实尾帧校正下一镜首帧。
- 续拍/延长：从上一条已验收视频的实际尾帧和未完成运动开始，不重演已完成动作。
- 结果修复：先归因，再只改一个主变量；不要直接重写成更长的 Prompt。
- 类型套用：按题材加载配方，但以用户剧情和参考素材为准。

## 按需读取

- 需要细化真人动作、微表情或多人反应时，读取 [references/performance-vocab.md](references/performance-vocab.md)。
- 需要把情绪或潜台词转成 L1–L4 表演时，读取 [references/emotion-library.md](references/emotion-library.md)。
- 需要选择景别、机位、焦段感或运镜时，读取 [references/camera-library.md](references/camera-library.md)。
- 需要宫斗、霸总、逆袭、悬疑、仙侠、带货等模板时，读取 [references/short-drama-recipes.md](references/short-drama-recipes.md)。
- 用户提供失败视频、截图或描述失败症状时，读取 [references/failure-atlas.md](references/failure-atlas.md)。
- 项目超过一个 clip、涉及续拍或镜头对切时，读取 [references/continuity-system.md](references/continuity-system.md)。
- 项目超过一个 clip、用户关心上下分镜自然衔接，或需要规划尾帧—首帧时，读取 [references/shot-seam-system.md](references/shot-seam-system.md)。
- 写任何超过一个主动作、两名角色或一个主运镜的短镜头前，读取 [references/complexity-scoring.md](references/complexity-scoring.md)。
- 用户需要实际拿去生图、生视频，或要求可执行生产步骤时，读取 [references/generation-handoff.md](references/generation-handoff.md)。

只读取当前任务需要的参考文件，不默认一次加载全部词库。

## Director's Read

写 Prompt 前，先在内部明确：剧情功能和注意力重点；人物目标、阻碍、权力关系与潜台词；情绪起点、触发、变化和终点；唯一主动作及可观察终点；摄影机的起点、运动理由与终点；必须保持的人物、服装、道具、空间和声音状态。

不要把内部分析标签直接塞进最终 Prompt；把它们翻译成镜头能看到或听到的行为。

## 编译原则

### 可见化

将“伤心、霸气、心虚、暧昧、震惊”等抽象词拆成视线目标、眼睑与眉部、嘴部、头部、呼吸、手势、身体重心、角色距离和节奏。每个情绪通常选 2–4 个互相支持的载体即可。

### 单一注意力

短镜头默认一个焦点人物、一个主动作、一个情绪转折、一个主运镜。其他角色保持反应层，不与主动作竞争。多人场景写明谁先动、谁后动、谁保持静止。

### 动作链

脆弱动作按顺序写：初始状态 → 触发 → 决定性变化 → 对方反应 → 收势 → 局部终点。手、脸、道具分别指定动作所有者，避免“大家同时动”。

### 镜头执行参数

这是最终视频 Prompt 的摄影部分，不是另附的说明。至少包含景别、机位、焦段感、主运镜、速度、与人物关系、起点和终点。短镜头不叠加相互竞争的推拉摇移跟环绕。对白和细微表演优先稳定机位或极缓单一移动。

### Prompt 编译完整性

“动作时间轴”和“镜头执行参数”是编写最终视频 Prompt 的源数据。交付前逐项核对：时间轴中的动作顺序、动作所有者、静止要求和动作终点，以及镜头参数中的景别、机位、焦段感、起点、运镜、速度、主体关系和终点，必须全部写进可复制的视频 Prompt。不能只出现在分析区。若某项仅供生图、剪辑、配音或验收使用，应在该项旁明确标注用途，不得让用户猜测是否需要提交给模型。

### 参考素材合同

每个 `@图片/@视频/@音频` 只设一个主要职责，并写明不应迁移的内容，例如：`@视频1仅参考运镜节奏，不迁移人物身份、服装和场景`。参考图中已存在的身份、服装和产品外观用“保持”约束，不在 Prompt 里反复重新描述。

### 连续性

计划状态不能覆盖已生成画面；验收后的可观察状态才是下一镜事实。记录动作终点、屏幕方向、视线对象、道具归属、服装细节、声音相位和摄影机相位。被拒绝的生成结果不进入连续性账本。

### 镜间接缝

多镜项目不能把各条 Prompt 独立设计。N 个 clip 必须规划 N−1 个镜间接缝。每个接缝先确定转场类型，再明确上一镜计划尾帧、下一镜计划首帧、必须继承项、允许变化项、跨镜动作和声音桥。生成后观察上一镜最后 0.5–1 秒，以实际稳定尾帧覆盖计划尾帧，再校正下一镜首帧和 Prompt。切换机位时继承剧情状态而非复制相同构图；同机位续拍才直接继承画面和未完成运动。

## 输出流程

根据请求规模交付最小充分结果：

1. 一句话导演判断：本镜意图与注意力中心。
2. 风险检查：复杂度分数、风险级别和是否拆镜。
3. 动作时间轴（将编入视频 Prompt）：多人镜头写谁在何时做什么、谁保持静止；单人镜头用动作链。
4. 镜头执行参数（将编入视频 Prompt）：列出景别、机位、焦段感、起点、运镜、速度、主体关系和终点。
5. 可复制 Prompt：自然中文，按“主体/状态 → 动作链 → 摄影 → 光线与环境 → 声音 → 保持项”组织；必须通过上述编译完整性核对。
6. 镜间接缝卡：多镜项目按相邻镜头输出；N个clip必须有N−1张，写清尾帧、首帧、继承项、允许变化、跨镜动作、声音桥和禁止跳变。
7. 连续性交接：输出本镜完成状态和下一镜起点；生成后以已验收真实尾帧回写并重新编译下一镜。
8. 可选保守版：仅用于高风险或返修场景，且只降低一个主要变量。

当用户要求“怎么生成”“拿去生图/生视频”或需要落地执行时，每个镜头追加 **AI生成操作卡**：需要准备的素材、推荐生成方式、首帧生图Prompt、可选尾帧Prompt、视频Prompt、实际操作步骤和验收标准。不能只给导演分析或一段无法判断素材编号的 Prompt。

## 面向用户的呈现规则

默认使用普通创作者能直接理解的中文，不把内部数据结构直接当成交付内容：

- 把“项目圣经”显示为 **拍摄设定卡**：说明画幅、时长、人物固定特征、服装、产品、场景位置和声音风格。
- 把“参考合同”显示为 **参考素材怎么用**：逐张写“这张图负责什么”和“不要从这张图复制什么”。
- 把“连续性账本”显示为 **本镜结束时必须保持的状态**：谁在哪里、看哪里、手里有什么、动作是否完成、下一镜从哪里开始。
- 把相邻镜头的交接显示为 **镜间接缝卡**：上一镜怎么结束、下一镜怎么开始、哪些必须保持、哪些可以改变；不要只写笼统的“保持连续”。
- 不向普通创作者显示“摄影合同”；改用 **镜头执行参数（已写入视频 Prompt）**。不把“表演与调度”单独悬空展示；改用 **动作时间轴（已写入视频 Prompt）**。
- 第一次出现 `L1–L4`、`正史`、`开放运动`、`轴线` 等术语时，用一句通俗中文解释；如果术语对交付没有帮助，直接不用。
- 不默认输出 YAML、JSON、字段名或内部 ID。只有用户明确要求机器可读格式、制作系统交接或批量自动化时，才在通俗版本之后附加结构化数据。
- 对尚未提供的参考图使用“皇帝参考图（如有）”等自然占位，不擅自宣称 `@图片1` 已经存在。用户给出素材后，再替换为界面中的准确引用标签。

## Prompt 骨架

```text
[模式与参考职责]。从[上一镜真实尾帧或本镜计划首帧]开始，继承[人物位置/手势/视线/道具/光线/声音]，允许[景别或机位等有意变化]。[焦点人物按顺序完成一个主动作，写明可见终点]；[配角的延迟反应或静止要求]。摄影机以[景别/机位]开始，[速度]地做[单一运镜]，始终与[人物/道具]保持[关系]，最后停在[可供下一镜继承的明确构图]。光线来自[物理光源与方向]，环境只保留[必要动态]。声音：[对白归属/环境音/声音桥/静默]。保持[身份、服装、道具、左右关系、文字或产品几何]不变；不得重演[已完成动作或对白]。
```

删掉无关项，保持动作因果和注意力清楚。

## 返修纪律

先指出证据，再命名根因，再移除冲突。一次返修只选择一个主变量：动作、机位、参考职责、身份锚点、时长/拆镜、声音约束之一。连续两次同类失败后，降低复杂度或拆镜；不要继续叠加形容词。

## 边界

- 不声称平台界面、参数、时长或能力永久不变；需要平台规格时先核实当前入口。
- 不把社区经验写成模型内部机制或官方保证。
- 对真人肖像、声音、品牌、版权角色和参考素材，确认用户拥有相应授权；只迁移被授权的身份或风格层。
- 本 Skill 负责导演设计、Prompt 编译与诊断；除非用户另行授权并提供生成工具，不自动提交付费生成任务。

